export interface ContactInfo {
  phone: string;
  whatsappNumber: string;
  email: string;
  instagram: string;
  instagramUsername: string;
  officeHours: string;
  location: string;
  oabNumber: string;
  creciNumber: string;
  creciFullText: string;
  fdvFullText: string;
}

export type PhotoMode = 'placeholder' | 'photo';
